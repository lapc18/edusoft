export interface InstitutionUser {
    id?: any;
    document: string;
    firstName: string;
    lastName: string;
    birthDay: string;
    birthPlace: string;
    email: string;
    phone: string;
}
